node {
    
    stage("pushing code from gitub"){
     git 'https://github.com/rupanandhs139/2021paytm-fridge.git'
        }
        stage("maven build"){
            sh 'mvn clean install'
        }
}
